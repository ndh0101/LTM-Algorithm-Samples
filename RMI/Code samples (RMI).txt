package "";
import java.rmi.registry.*;
import java.util.*;
import java.math.*;
import RMI.?Service;
import RMI.Model;
public class ClientRMI {
    public static void main(String args[]) throws Exception{
        String studentCode="B22DCCN...",qCode=".........";
        Registry registry=LocateRegistry.getRegistry("203.162.10.109");
	// ? = Byte, Character, Data, Object
        ?Service service=(?Service)registry.lookup("RMI?Service");

	// Nếu là RMIObjectService
        Model model=(Model)service.requestObject(studentCode,qCode);
        System.out.println(model);

        service.submit?(studentCode,qCode,?);
    }
}